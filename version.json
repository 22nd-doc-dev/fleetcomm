{
  "version": "0.12.10",
  "notes": "COMMAND can actually add clips to the 1MC sound library now. Adding clips failed with \"COMMAND authority required\" even for COMMAND accounts: the app checked your rank against a field the sign-in server has never sent, so the answer was always no — the server would have accepted the upload, but your own app refused to ask. The app now keys that check on the role the server actually reports. Update, sign in with Discord as usual, and ADD CLIPS under SYS → 1MC SOUND LIBRARY works."
}
