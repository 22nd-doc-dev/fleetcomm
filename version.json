{
  "version": "0.10.1",
  "notes": "Authentication startup hotfix: restores the Discord sign-in screen and Connect action by permitting FleetComm's bundled Opus codec to initialize inside the otherwise isolated local renderer. A visible startup diagnostic now replaces the misleading legacy callsign fallback if initialization ever fails again."
}
