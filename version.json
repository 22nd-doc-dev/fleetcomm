{
  "version": "1.4.5",
  "notes": "FleetComm 1.4.5 — joint task force. COMMAND: ACCOUNTS & ACCESS has an ALLIED ORGANIZATIONS block — add an allied group by its Discord server id and its people sign in with their own Discord as ALLIED, no approval queue; mark the joint nets JOINT in NET ACCESS and that is all they can reach — the relay enforces it. Allied operators see only the joint nets, with a banner naming their org. Also: technical lines (audio engine, microphone, hooks, cam links) now live under SETTINGS ▸ SYSTEM LOG with a COPY button; the COMM LOG keeps radio traffic. Runbook for the day: docs/JTF-RUNBOOK.md in the repo."
}
